let mix: string | number;

mix = 'Hello';
console.log('mix (string):', mix);

mix = 123;
console.log('mix (number):', mix);

let OneOrTwo: 'enable' | 'disable';
OneOrTwo = 'enable';

console.log('OneOrTwo:', OneOrTwo);
